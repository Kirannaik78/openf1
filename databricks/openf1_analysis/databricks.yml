# This is a Databricks asset bundle definition for openf1_analysis.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: openf1_analysis
  uuid: 0b019c04-1e00-4e16-8355-9465b0c7d9b6

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: /home/kiran/.pyenv/shims/uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1506361690816079.19.azuredatabricks.net
    variables:
      catalog: dev
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-1506361690816079.19.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/kiranrnaik78@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/kiranrnaik78@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev
      schema: prod
    permissions:
      - user_name: kiranrnaik78@gmail.com
        level: CAN_MANAGE
